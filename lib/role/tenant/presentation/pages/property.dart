import 'package:flutter/material.dart';

class TenantPropertyPage extends StatelessWidget {
  const TenantPropertyPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('Tenant Property'));
  }
}
